function displayResult() {
    const result = "Hello, welcome to the " + document.title + " page!";
        
    // Display result in the div with id 'output'
    document.getElementById("display-function-output").textContent = result;
}
